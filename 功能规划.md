# BMS 毕业设计功能规划

> 本文档记录 BMS 项目在基础版本之上新增的技术功能，供开发参考与答辩说明使用。
> 最后更新：2026-04-08

---

## 项目基础信息

| 项目 | 说明 |
|------|------|
| 硬件平台 | STM32G491RETx（ARM Cortex-M4F，170 MHz） |
| 开发板 | NUCLEO-G491RE |
| 传感器 | INA228（电流/电压，I2C2）、NTC热敏电阻（ADC1） |
| 显示 | SSD1306 OLED 128×64（I2C3） |
| 负载控制 | MOSFET（GPIO PB5） |
| 操作按键 | 板载 B1（单按钮） |
| 电池 | 3S LiPo（9.0V–12.6V，标称 2.2 Ah） |

---

## 新增功能总览

| 序号 | 功能名称 | 类别 | 技术难度 | 来源 |
|------|---------|------|---------|------|
| F1 | 开机倒计时与传感器预热 | 用户体验 | 低 | 自提 |
| F2 | 单按钮阈值调整（设置模式） | 交互 | 中 | 自提 |
| F3 | SOC 估算升级（互补滤波） | 算法 | 中 | 建议采纳 |
| F4 | 预测性故障保护（dT/dt） | 算法 | 低-中 | 建议采纳 |
| F5 | SOC 可视化（图标 + 历史曲线） | 显示 | 中 | 建议采纳 |
| F6 | Flash 统计数据记录 | 系统 | 低 | 建议采纳 |

---

## 各功能详细说明

---

### F1 · 开机倒计时与传感器预热

**背景**

INA228 配置了高倍数 ADC 平均采样，首次完整采样需要约 1 秒以上。若直接进入主循环，第一次 OCV 读数可能不稳定，影响 SOC 初始化精度。

**功能描述**

上电后 OLED 先显示启动页面，倒计时 3 秒，期间完成传感器稳定等待，倒计时结束后进入正常工作界面。

**OLED 显示效果**

```
┌────────────────┐
│  BMS  v4.0     │
│ Sensor warming │
│                │
│       3        │  ← 每秒更新为 2、1
└────────────────┘
```

**实现要点**

- 使用 `HAL_GetTick()` 计时，不阻塞主循环
- 倒计时期间每秒刷新一次数字
- 倒计时结束后再执行第一次 INA228 采样和 SOC OCV 初始化

**技术价值**

解决传感器冷启动误差问题，提升 SOC 初始化准确性。

---

### F2 · 单按钮阈值调整（设置模式）

**背景**

当前所有保护阈值为编译期常量（`#define`），修改需要重新烧录固件。在实际部署场景中，用户需要根据应用场景微调保护参数。

**功能描述**

通过长按 B1 进入设置模式，在当前显示页面上调整对应阈值，调整结果保存至 Flash，断电不丢失。

**交互设计（基于松手时长判断）**

| 按压时长（松手触发） | 普通模式 | 设置模式 |
|---|---|---|
| < 0.8s | 切换页面 | 数值减小一步 |
| 3s – 5s | 进入设置模式 | 数值增大一步 |
| > 5s | 进入设置模式 | 保存并退出 |
| 无操作 8s | — | 自动退出（不保存） |

按住期间 OLED 底部显示实时进度条，标记 0.8s / 3s / 5s 临界点。

**可调阈值列表（第一期）**

| 所在页面 | 阈值 | 默认值 | 最小 | 最大 | 步长 |
|---------|------|--------|------|------|------|
| TEMP 页 | MOSFET 截止温度 | 30°C | 15°C | 50°C | 5°C |
| CURR 页 | 过流保护电流 | 400 mA | 100 mA | 800 mA | 50 mA |
| SOC 页 | 低电量警告阈值 | 30% | 5% | 50% | 5% |

暂不开放：故障温度 60°C（安全硬限制）、欠流阈值 6 mA。

**Flash 存储格式**

```
偏移 0x00: Magic 校验字（0xBEEFCAFE）
偏移 0x04: mosfet_off_temp (float)
偏移 0x08: overcurrent_a (float)
偏移 0x0C: soc_low_thresh (float)
偏移 0x10: checksum (前三个 float 之和的整数部分)
```

使用 STM32G491 Flash 末尾一页（0x0807F800，2KB），启动时校验 magic，失败则恢复默认值。

**技术价值**

运行时参数持久化、状态机设计、Flash EEPROM 模拟应用。

---

### F3 · SOC 估算升级（互补滤波）

**背景**

现有方案"OCV 初始化 + 库仑计数"有两个已知弱点：
1. 库仑计数存在积分漂移，误差随时间累积
2. OCV 校正仅在小电流时触发，属于离散修正

**升级方案：互补滤波**

将 OCV 估算和库仑计数两路结果按动态权重融合：

```
SOC_fused = α × SOC_coulomb + (1 - α) × SOC_ocv
```

权重 α 根据当前电流大小自适应调整：

| 工况 | α 值 | 含义 |
|------|------|------|
| 大电流放电（> 200 mA） | ≈ 0.95 | 主要信任库仑计数 |
| 中等电流（50–200 mA） | 0.7–0.9 | 两者平衡 |
| 小电流（< 50 mA） | ≈ 0.5 | OCV 可信，加大权重 |

**与卡尔曼滤波的关系**

互补滤波是卡尔曼滤波在线性时不变系统下的简化形式，工程实现复杂度低，但理论依据相同，适合在报告中与卡尔曼滤波理论做对比说明。

**技术价值**

多源数据融合、自适应算法、提升 SOC 长期精度，是 BMS 领域核心算法之一。

---

### F4 · 预测性故障保护（温度变化率 dT/dt）

**背景**

现有保护逻辑为**被动阈值触发**：温度超过 60°C 才报警，属于事后响应。在温度快速上升的情况下，可能来不及保护。

**升级方案：基于变化率的预警**

计算温度对时间的变化率（差分近似）：

```
dT/dt = (T_now - T_prev) / Δt   [单位：°C/s]
```

在超过阈值**之前**，提前触发预警：

| 条件 | 动作 |
|------|------|
| dT/dt > 0.5°C/s，且当前温度 > 20°C | OLED 显示 "TEMP RISING" 预警 |
| dT/dt > 1.0°C/s | 提前关闭 MOSFET（预防性断开） |
| 温度已超 30°C（原有逻辑） | 正常触发 MOSFET 截止 |

使用滑动窗口（最近 5 个采样点）平滑 dT/dt，避免单次噪声误触发。

**技术价值**

导数控制思想（与 PID 中 D 项同源）、预测性保护逻辑、提升系统安全裕度。

---

### F5 · SOC 可视化（电池图标 + 历史趋势曲线）

**背景**

当前 SOC 页面只显示纯数字百分比，信息密度低，对演示场景说服力不足。

**功能描述：两种可视化模式**

**① 电池图标（替换纯数字）**

```
┌────────────────┐
│ SOC            │
│ [████████░░]   │  ← 按 SOC 比例填充
│   80%          │
└────────────────┘
```

图标宽 20 格，按当前 SOC 填充实心块，SOC < 30% 时闪烁警告。

**② 历史趋势曲线（新增）**

保存最近 64 个 SOC 采样点（每 5 秒一个，约 5 分钟历史），在 OLED 的下半区（32 行像素）绘制折线图：
- X 轴：时间（最近 5 分钟）
- Y 轴：SOC 0–100%
- 每个数据点对应 2 个像素列

```
┌────────────────────────────┐
│ SOC  80%                   │
│ [████████░░░░]             │
│····●                       │  ↑
│·····●●                     │  历
│·······●●●                  │  史
│··········●●●●●●●●         │  曲
└────────────────────────────┘  线
```

**实现要点**

- 需要扩展 SSD1306 驱动，增加 `DrawPixel(x, y)` 和 `DrawLine()` 两个基础函数
- 历史数据用循环缓冲区（ring buffer）管理，固定 64 个 float 槽位

**技术价值**

像素级图形渲染、循环缓冲区数据结构、嵌入式 UI 设计。

---

### F6 · Flash 统计数据记录

**背景**

系统目前没有任何跨上电周期的数据记录，无法了解电池的使用历史和健康趋势。

**统计项目**

| 字段 | 说明 | 数据类型 |
|------|------|---------|
| 累计使用时长 | 每次开机后累加 | uint32_t（分钟） |
| 历史最高温度 | 运行期间记录 max | float（°C） |
| 累计放电能量 | ∫(V×I)dt 积分 | float（Wh） |
| 上次关机时 SOC | 用于下次开机直接恢复 | float（0–1） |
| 开机次数 | 每次上电 +1 | uint16_t |

**开机 SOC 恢复逻辑**

若上次关机 SOC 与当前 OCV 估算误差 < 5%，直接使用上次关机值作为初始 SOC，跳过 OCV 查表，减少首次采样误差。

**Flash 布局**（与 F2 阈值数据共用同一 Flash 页）

```
偏移 0x00–0x13：阈值设置数据（F2 部分，20 字节）
偏移 0x14–0x2F：统计数据（28 字节）
偏移 0x30：统计数据校验字
```

**OLED 新增统计页**（可选，B1 翻页到达）

```
┌────────────────┐
│ STATS          │
│ Time:  128 min │
│ MaxT:  42.1°C  │
│ Energy:3.2 Wh  │
└────────────────┘
```

**技术价值**

非易失性数据管理、Flash EEPROM 模拟、跨会话状态恢复。

---

## 实现优先级与建议顺序

```
第一阶段（基础体验）
  F1 开机倒计时          ← 简单，先做，立刻改善上电体验

第二阶段（核心算法）
  F3 SOC 互补滤波        ← 技术含量最高，优先完成
  F4 dT/dt 预测性保护    ← 与 F3 同期，代码量少

第三阶段（系统完善）
  F2 单按钮阈值调整      ← 依赖 Flash 模块，与 F6 一起做
  F6 Flash 统计记录      ← 与 F2 共用 Flash 层，合并实现

第四阶段（显示升级）
  F5 SOC 可视化          ← 最后做，需要扩展 SSD1306 驱动
```

---

## 技术亮点汇总（供答辩参考）

1. **多源数据融合**：互补滤波将 OCV 与库仑计数动态加权，兼顾短期精度和长期稳定性
2. **预测性保护**：dT/dt 变化率检测，在故障发生前触发预警，体现主动安全设计
3. **人机交互**：单按钮多级状态机，实现无额外硬件的参数配置功能
4. **数据持久化**：Flash EEPROM 模拟，实现阈值和历史统计的非易失性存储
5. **嵌入式图形**：128×64 像素级 SOC 趋势曲线，展示自定义图形渲染能力
